# Desktop app source files — these belong in the BPTV2 repo, not the website
*.cs
*.csproj
*.sln
*.xaml
*.xaml.cs
appsettings*.json

.firebase/

node_modules/
package-lock.json


WEBSITE.zip

# Firebase local artifacts
*.tgz
firebase-debug.log

# Stale local copy of RTDB rules (canonical copy lives at repo root and is referenced by firebase.json)
public/database.rules.json

